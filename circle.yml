# The deployment section should be excluded until
# it is explicitly included in module 5

#deployment:
#  dockerhub:
#    branch: master
#    commands:
#      curl -H "Content-Type: application/json" --data'{"build":true}' -X POST <https://registry.hub.docker.com/u/frednbt/pluralsight-docker-ci/trigger/7df23a8c-192d-48dc-846b-780bb94555fa/>

test:
  override:    
    - mocha
